% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tables.R
\name{bt_kable}
\alias{bt_kable}
\title{Render a kable table with Bio Tooltips support}
\usage{
bt_kable(
  data,
  ...,
  modules = c("gene", "chemical"),
  include_setup = TRUE,
  format = "html",
  escape = FALSE
)
}
\arguments{
\item{data}{A data frame or object accepted by \code{\link[knitr:kable]{knitr::kable()}}.}

\item{...}{Passed to \code{\link[knitr:kable]{knitr::kable()}}.}

\item{modules}{Tooltip modules to initialize when \code{include_setup = TRUE}.}

\item{include_setup}{Include \code{\link[=use_bio_tooltips]{use_bio_tooltips()}} before the table.}

\item{format}{Table format. Defaults to \code{"html"}.}

\item{escape}{Escape HTML? Defaults to \code{FALSE} so tooltip spans render.}
}
\value{
An HTML tag list when setup is included; otherwise the result of
\code{\link[knitr:kable]{knitr::kable()}}.
}
\description{
Wrapper around \code{\link[knitr:kable]{knitr::kable()}} that defaults to HTML output with escaping
disabled and optionally prepends \code{\link[=use_bio_tooltips]{use_bio_tooltips()}}.
}
\examples{
top_genes <- data.frame(symbol = c("TP53", "BRCA1"))
top_genes <- gene_column(top_genes, symbol)
bt_kable(top_genes)
}
